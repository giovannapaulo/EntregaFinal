name: sensivapp_final
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  table_calendar: ^3.1.2
  flutter_map: ^8.2.1
  latlong2: ^0.9.1
  geolocator: ^14.0.1
  http: ^1.2.1
  image_picker: ^1.1.2
  url_launcher: ^6.3.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true